#include <stdio.h>
#include <stdlib.h>
char f(int x);


int main(){

char x = f(6);

printf("resposta: %c.\n",x);


return 0;

}


char f(int x) {

if(x==1) return('c');
else if(x==2) return('d');
else if(x==3) return('e');
else if(x==4) return('b');
else if(x==5) return('e');
else{ 
    
    printf("A funçao nao esta definida para X = %d, x");

exit(1);
}
}

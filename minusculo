#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

char minusculo ();

int main(){

    system("cls"); //limpa a tela//
char letra = minusculo();
printf("\n%c", letra);


    
}

char minusculo (){

char ch= getche(); //só lê um caractere//
if ((ch >= 'A') && (ch <= 'Z')) return(ch +32);
else return (ch);

}
